## App environment (no secrets)

# Required for Prisma datasource
# Example (Postgres): postgresql://user:pass@localhost:5432/conditioner
# Example (SQLite): file:./dev.db
DATABASE_URL=

# NextAuth configuration
# Generate a random string in dev, e.g. `openssl rand -base64 32`
NEXTAUTH_SECRET=
# Base URL of the app (used by NextAuth)
# e.g., http://localhost:3000 during local dev
NEXTAUTH_URL=

# Optional: AWS credentials (if using Polly TTS)
#AWS_ACCESS_KEY_ID=
#AWS_SECRET_ACCESS_KEY=
#AWS_REGION=us-east-1

# Optional: Dev probe script settings
#PROBE_PORT=3100
#PROBE_HOST=0.0.0.0
